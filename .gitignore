node_modules
dist
.vercel
*.local
.DS_Store

# Secrets — never commit real keys
.env
.env.*
!.env.example

# Local machine-specific dev helpers (portable Node paths)
.claude/launch.json
run-dev.cmd
dev.log
